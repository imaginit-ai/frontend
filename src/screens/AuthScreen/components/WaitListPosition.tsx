import { WaitlistUser } from "@/types";

type WaitListPositionProps = {
  userData: WaitlistUser | undefined;
};

const WaitListPosition = (props: WaitListPositionProps) => {
  return <div></div>;
};

export default WaitListPosition;
